const initialState = {
    // Usar string vazia ao fazer o Build
    // Usar http://localhost ao usar o npm start
    caminho: "http://localhost"
}

const caminhoReducer = (state = initialState) => {
    return state
}

export default caminhoReducer;